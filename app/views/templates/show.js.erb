$('#confirmModal .title').html("<%= t('template.templates') %>");
$('#confirmModal .modal-body').html("<%= t('template.are_you_sure_to_delete', name: @template.name).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= template_path(@template) %>");
$("#confirmModal").modal("show");
