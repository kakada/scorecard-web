.logo-wrapper.p-5.mr-5
  %h3.title.mb-3.text-center
    = ENV['APP_NAME']

    - if shortcut_name = ENV['APP_SHORTCUT_NAME'].presence
      = "(#{shortcut_name})"

  .logo-image-wrapper.flex-row.text-center.justify-content-conter
    = image_tag 'csc_logo.png', size: '120x120'

  %footer
    .app-description.rounded.my-5.p-3{ style: "background-color: #fff;" }
      %h6.font-weight-bold សម្រាប់អ្នកមិនមានគណនីហើយដែលចង់ស្វែងយល់បន្ថែម
      %p if you don't have an account and want to learn more

      %h6.font-weight-bold សូមទាក់ទងមកយើងតាមរយៈ contact@digitalcsc.org
      %p Please reach out to use via: contact@digitalcsc.org

    = render 'devise/shared/stackholders'