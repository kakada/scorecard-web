$('#confirmModal .title').html("<%= t('pdf_template.pdf_template') %>");
$('#confirmModal .modal-body').html("<%= t('pdf_template.are_you_sure_to_delete', name: @pdf_template.name).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= pdf_template_path(@pdf_template) %>");
$("#confirmModal").modal("show");
