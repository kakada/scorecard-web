$('#confirmModal .title').html("<%= t('indicator.indicator_action') %>");
$('#confirmModal .modal-body').html("<%= t('indicator_action.are_you_sure_to_delete', name: @indicator_action.name).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= indicator_indicator_action_path(@indicator, @indicator_action) %>");
$("#confirmModal").modal("show");
