- if flash[:notice] == t('scorecard.create_successfully')
  #scorecardModal.modal{:role => "dialog", :tabindex => "-1"}
    .modal-dialog{:role => "document"}
      .modal-content
        .modal-header
          %h5.modal-title Scorecard
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
        .modal-body
          %p
            %span.text-primary
              = @scorecard.uuid
              = render 'scorecard_copy', scorecard: @scorecard

            %sapn ប្រើលេខកូដនេះសម្រាប់ទាញយក Scorecard
        .modal-footer
          %button.btn.btn-secondary{"data-dismiss" => "modal", :type => "button"}= t("shared.close")
