#facility.filter-input{'data-toggle' => 'multiSelect'}
  / Trigger
  .tooltips{'data-toggle' => 'tooltip'}
    / Require hidden-input class
    %input.hidden-input{ name: 'facility_ids', type: :hidden, value: params[:facility_ids] }

    / Require div for display
    .form-control.text-truncate{'data-toggle' => 'modal', 'data-target' => '#facilityModal', 'data-placeholder' => t('scorecard.any_facility'), 'data-display-text' => t('scorecard.n_facility_selected')}

  / Modal
  #facilityModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "exampleModalLabel", :role => "dialog", :tabindex => "-1"}
    .modal-dialog{:role => "document"}
      .modal-content
        .modal-header
          %div
            %h5#exampleModalLabel.modal-title= t('scorecard.facility')
            %small.text-muted.font-italic= "(#{t('scorecard.can_select_multiple_facility')})"

          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×

        .modal-body
          - list = current_program.facilities.leaves.map{|l| {value: l.id, name: l["name_#{I18n.locale}"]}}

          / Require tagify-input class
          %input.tagify-input{ placeholder: t('scorecard.input_facility_name'), 'data-list' => list.to_json }

        .modal-footer
          %button.btn.btn-secondary{"data-dismiss" => "modal", :type => "button"}= t('shared.cancel')

          / Require btn-save class
          %button.btn.btn-primary.btn-save{"data-dismiss" => "modal", :type => "button"}= t('shared.save')
