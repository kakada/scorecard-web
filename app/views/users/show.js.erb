$('#confirmModal .title').html("<%= t('user.users') %>");
$('#confirmModal .modal-body').html("<%= t('user.are_you_sure_to_delete', email: @user.email).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= user_path(@user) %>");
$("#confirmModal").modal("show");
