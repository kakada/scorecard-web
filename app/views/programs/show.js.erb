$('#confirmModal .title').html("<%= t('program.programs') %>");
$('#confirmModal .modal-body').html("<%= t('program.are_you_sure_to_delete', name: @program.name).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= program_path(@program) %>");
$("#confirmModal").modal("show");
