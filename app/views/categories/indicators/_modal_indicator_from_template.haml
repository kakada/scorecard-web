%button.btn.btn-success{"data-target" => "#exampleModal", "data-toggle" => "modal", :type => "button"}
  Import from template
/ Modal
#exampleModal.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "exampleModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %h5#exampleModalLabel.modal-title Import from template
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×

      = form_for '', :url => import_from_template_category_indicators_path(@category) do |f|
        .modal-body
          .form-group
            %label= t('indicator.template')
            = select_tag "template_id", options_from_collection_for_select(current_program.templates, "id", "name"), class: 'form-control'

        .modal-footer
          %button.btn.btn-secondary{"data-dismiss" => "modal", :type => "button"}= t('shared.cancel')
          %button.btn.btn-primary{:type => "submit"}= t('shared.done')
