= f.input :code, label: t('language.code')
= f.input :name, label: t('language.name')

.actions
  = f.submit t('shared.save'), class: 'btn btn-primary'
  = link_to t('shared.cancel'), languages_path, class: 'btn btn-secondary'
