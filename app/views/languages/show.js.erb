$('#confirmModal .title').html("<%= t('language.languages') %>");
$('#confirmModal .modal-body').html("<%= t('language.are_you_sure_to_delete', name: @language.name).html_safe %>");
$("#confirmModal .btn-yes").attr('href', "<%= language_path(@language) %>");
$("#confirmModal").modal("show");
